import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sundarbandraghouse.app',
  appName: 'সুন্দরবন ড্রাগ হাউস',
  webDir: 'www',
  server: { androidScheme: 'https' }
};

export default config;
